import { Input } from "@/components/ui/input"

export const ChatInput = () => {
    return (
        <div>
            <Input placeholder="Type a message" className="bg-primary/10" />
        </div>
    )
}